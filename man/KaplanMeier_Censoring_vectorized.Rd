% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/KaplanMeier_Censoring_vectorized.R
\name{KaplanMeier_Censoring_vectorized}
\alias{KaplanMeier_Censoring_vectorized}
\title{Kaplan-Meier Estimator for Censoring Distribution (Vectorized)}
\usage{
KaplanMeier_Censoring_vectorized(times, status, unique_times)
}
\arguments{
\item{times}{A numeric vector of observed times.}

\item{status}{A numeric vector of status indicators (0 = censored, >0 = event).}

\item{unique_times}{A numeric vector of time points at which to evaluate the estimator.}
}
\value{
A numeric vector of estimated survival probabilities at each unique time.
}
\description{
Computes the Kaplan-Meier estimator of the censoring distribution at given time points.
}
\examples{
\dontrun{
n_cov <- 1
n_cluster <- 5
n_per_cluster <- 100
n <- n_cluster * n_per_cluster
G <- rep(1:n_cluster, each = n_per_cluster)
Z <- matrix(runif(n * n_cov), ncol = n_cov)
df <- simulate_data(G, Z, prop = 0.6, beta = c(0), theta = 0.01, cens = TRUE, pcens = 0.25, tau = 0)
check_data_format(df)
unique_times <- sort(unique(df$times))
KM <- KaplanMeier_Censoring_vectorized(df$times, df$status, unique_times)
}

}
